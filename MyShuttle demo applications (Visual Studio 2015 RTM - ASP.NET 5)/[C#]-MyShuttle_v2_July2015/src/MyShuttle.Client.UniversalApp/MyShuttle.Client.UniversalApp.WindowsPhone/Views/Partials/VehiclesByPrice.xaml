<UserControl
    x:Class="MyShuttle.Client.UniversalApp.Views.Partials.VehiclesByPrice"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:MyShuttle.Client.UniversalApp.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverterKey"/>
    </UserControl.Resources>

    <Grid>
        <ProgressRing Visibility="{Binding IsLoadingFilteredVehicles, Converter={StaticResource BooleanToVisibilityConverterKey}}"
                      Style="{StaticResource WhiteProgressRing}"/>

        <ListView Name="CheapestVehiclesListView" 
                  ItemTemplate="{StaticResource CheapestVehiclesItemTemplate}" 
                  ItemContainerStyle="{StaticResource VehiclesItemContainer}" 
                  ItemsSource="{Binding FilteredVehicles}"/>
    </Grid>
</UserControl>
